# PlanB Market — 개발 진행 계획

> Claude Code에 **한 단계씩** 지시하고, 단계마다 직접 확인한 뒤 다음으로 넘어간다.
> 한 번에 전부 시키면 3~4단계쯤에서 무너진다.

---

## 전체 일정

| 단계 | 내용 | 예상 | 누적 |
|---|---|---|---|
| 1 | 프로젝트 셋업 + 엔티티 + Repository | 2.5h | 2.5h |
| 2 | 기본 CRUD + 예외 + Validation + Swagger | 2.5h | 5.0h |
| 3 | **에스크로 + 원장** 고비 | 3.0h | 8.0h |
| 4 | 예약금 | 2.5h | 10.5h |
| 5 | 1매 대기 매칭 + 부분성사 폴백 | 3.0h | 13.5h |
| 6 | 스케줄러 + 알림 | 2.5h | 16.0h |
| 7 | MyBatis 통계 3종 | 2.0h | 18.0h |
| 8 | 동시성 락 + 검증 | 2.0h | 20.0h |
| 9 | Actuator + AOP + 대시보드 | 3.5h | 23.5h |
| 10 | 전체 검증 + 캡처 | 2.0h | 25.5h |
| 11 | PDF 보고서 | 3.0h | 28.5h |

**여유가 거의 없다.** 3단계에서 막히면 4·5단계를 축소할 각오를 미리 해둘 것.

---

## 단계별 상세

### 1단계 — 프로젝트 셋업 + 엔티티

**시킬 것**
- Spring Boot 3.3.x / Java 21 / Gradle 프로젝트 생성
- 의존성: web, data-jpa, mybatis, validation, aop, actuator, lombok, h2, springdoc-openapi
- `application.yml` — H2 인메모리, JPA `ddl-auto: create`, H2 콘솔 활성화
- 엔티티 8개 + enum 전부
- JPA Repository 8개 (기본만)
- `data.sql` 시드 데이터

**O 직접 확인**
- [ ] `./gradlew bootRun` 이 뜬다
- [ ] `http://localhost:8080/h2-console` 접속 → 테이블 8개 생성됨
- [ ] 시드 데이터가 들어가 있다 (회원 5, 티켓 20, 과거거래 15)
- [ ] `Ticket.expiresAt` 이 카테고리별로 올바르게 계산되어 있다

**[캡처] 캡처**: `01_h2_테이블목록.png`, `02_시드데이터.png`

---

### 2단계 — 기본 CRUD + 공통

**시킬 것**
- `Response`, `PagedList`, `SessionHandler`(세션 기반)
- `Error` enum, `ResponseException`, `ParameterException`, `GlobalExceptionHandler`
- Member / Ticket / Listing CRUD + 로그인
- Bean Validation (`@NotBlank`, `@Min`, `@Future` 등)
- Swagger 설정 — 태그를 기능별로 분리

**O 직접 확인**
- [ ] Swagger UI 접속됨 (`/swagger-ui/index.html`)
- [ ] 회원가입 → 로그인 → 세션 유지되는지
- [ ] 티켓 등록 → 판매 등록까지 되는지
- [ ] **잘못된 입력 시 400 + 필드별 오류 메시지**가 나오는지
- [ ] 없는 ID 조회 시 404 나오는지

**[캡처] 캡처**: `03_swagger_전체API.png`, `04_validation_400.png`, `05_404.png`

---

### 3단계 — 에스크로 + 원장 (최대 고비)

**시킬 것**
- `LedgerService` — 모든 금전 이동을 append-only 기록. **UPDATE 금지**
- `EscrowService` — 2매 단독 구매 흐름
  - 잔액 확인 → 차감 → 에스크로 생성 → 원장 2줄(구매자 DEBIT, 에스크로 보관)
- 구매 확정 → 판매자 정산 → 원장 기록
- 환불 → 원장 역기록
- `GET /api/admin/integrity-check` 정합성 검증

**O 직접 확인 (여기서 안 맞으면 절대 다음으로 넘어가지 말 것)**
- [ ] 구매 후 구매자 잔액이 정확히 차감됐다
- [ ] 확정 후 판매자 잔액이 정확히 증가했다
- [ ] `integrity-check` 가 `ledgerBalanced: true` 를 반환한다
- [ ] 환불 후에도 잔액과 원장이 맞는다
- [ ] 본인 티켓 구매 시 400 나온다
- [ ] 잔액 부족 시 400 나온다

**[캡처] 캡처**: `06_구매_전후잔액.png`, `07_원장조회.png`, `08_정합성검증_PASS.png`

---

### 4단계 — 예약금

**시킬 것**
- `Deposit` 엔티티 + `DepositService`
- HOLD / CAPTURED / FORFEITED / RELEASED 4상태 전이
- 각 전이마다 원장 기록
- 청약철회 10분 룰

**O 직접 확인**
- [ ] 예약금 홀드 시 잔액이 줄고 원장에 `DEPOSIT_HOLD` 기록됨
- [ ] 10분 내 취소 → 전액 환불, 잔액 복구
- [ ] 몰수 시 PLATFORM 계정으로 이동, 원장 균형 유지
- [ ] `integrity-check` 여전히 PASS

**[캡처] 캡처**: `09_예약금_홀드.png`, `10_예약금_몰수.png`

---

### 5단계 — 1매 대기 매칭 + 부분성사

**시킬 것**
- `POST /api/listings/{id}/pair-requests` — 대기 등록 (예약금 홀드)
- 2명 충족 시 → `MATCHED` + 양쪽에 알림 + `paymentDeadline` 설정
- `POST /api/pair-requests/{id}/pay` — 결제
- 부분성사: 한쪽만 결제 → 제한시간 초과 시
  - 미결제자 예약금 몰수
  - 결제자에게 2매 전체 구매 제안
  - 수락 → 몰수액 50% 크레딧 / 거절·무응답 → 전액 환불
- `GET /api/listings/{id}/pair-status` (participants는 빈 배열)

**O 직접 확인**
- [ ] pairable=false 카테고리에 대기 신청 시 400
- [ ] 1명 대기 → `waitingCount: 1` 표시됨
- [ ] 2명 충족 → `MATCHED` 전이 + 알림 2건 생성
- [ ] 양쪽 결제 → 에스크로 2건 생성, 예약금 CAPTURED
- [ ] 한쪽만 결제 → 제안 알림 발송
- [ ] 수락 시 크레딧 적용된 금액으로 결제됨
- [ ] **판매자는 항상 정가를 받는다**
- [ ] `integrity-check` PASS

**[캡처] 캡처**: `11_대기등록.png`, `12_대기현황.png`, `13_성사알림.png`, `14_부분성사_제안.png`, `15_크레딧적용.png`

---

### 6단계 — 스케줄러 + 알림

**시킬 것**
- `ExpiryScheduler` (1분) — 만료 티켓 처리 + 상태별 환불
- `PaymentDeadlineScheduler` (1분) — 결제 제한시간 초과 처리
- `AutoConfirmScheduler` (5분) — 자동 확정
- 만료 임박 경고 알림 (24시간 전, 1회)
- 알림 API 4종

**O 직접 확인**
- [ ] 시드의 "이미 만료" 티켓 3건이 `EXPIRED` 로 바뀐다
- [ ] 만료 시 대기자 예약금이 전액 환불된다
- [ ] 결제 제한시간 초과 시 자동으로 몰수 처리된다
- [ ] 알림이 쌓이고 읽음 처리가 된다
- [ ] `integrity-check` PASS

> 테스트 팁: 시드에 만료 2분 후 티켓을 하나 넣어두면 스케줄러 동작을 실시간으로 볼 수 있다.

**[캡처] 캡처**: `16_스케줄러로그.png`, `17_알림목록.png`, `18_만료실효.png`

---

### 7단계 — MyBatis 통계 3종

**시킬 것**
- `AnalysisMapper` + XML
- 가격 추천 / 카테고리별 현황 / 일별 실효 손실
- 표본 0건 폴백 로직

**O 직접 확인**
- [ ] 가격 추천 결과에 `sampleCount` 가 나온다
- [ ] 표본 0건인 카테고리도 에러 없이 폴백값을 준다
- [ ] 카테고리별 실효율 숫자가 말이 된다
- [ ] 일별 손실 집계가 실제 실효 건과 일치한다

**[캡처] 캡처**: `19_가격추천.png`, `20_카테고리현황.png`, `21_실효손실.png`

---

### 8단계 — 동시성 락 (가장 중요)

**시킬 것**
- Listing / Member 비관적 락 Repository 메서드
- 락 획득 순서 고정 (Listing → 구매자 → 판매자)
- `POST /api/admin/simulate-concurrent` — `useLock` 플래그로 on/off

**O 직접 확인**
- [ ] `useLock: false` → 중복 거래 발생, `dataIntegrity: false`
- [ ] `useLock: true` → 1건만 성공, 나머지 `ALREADY_SOLD`
- [ ] 두 결과가 명확히 다르다

**[캡처] 캡처**: `22_동시성_락없음.png`, `23_동시성_락적용.png` ← **PDF 하이라이트**

---

### 9단계 — Actuator + AOP + 대시보드

**시킬 것**
- `ApiLogAspect`, `TradeAuditAspect`
- 커스텀 HealthIndicator 2종 + 커스텀 메트릭 4종
- `static/index.html` 대시보드 (다크 테마, 카운트다운, 차트, 검증 버튼)

**O 직접 확인**
- [ ] `/actuator/health` 에 커스텀 항목이 보인다
- [ ] `/actuator/metrics/planb.escrow.created` 가 실제 값을 반환한다
- [ ] AOP 로그가 콘솔에 찍힌다
- [ ] 대시보드가 뜨고 카운트다운이 실시간으로 줄어든다
- [ ] 정합성 검증 버튼이 동작한다
- [ ] 동시성 테스트 버튼이 동작한다

**[캡처] 캡처**: `24_actuator_health.png`, `25_actuator_metrics.png`, `26_AOP로그.png`, `27_대시보드_전체.png`, `28_대시보드_카운트다운.png`

---

### 10단계 — 전체 검증

**필수 시나리오 6개를 처음부터 끝까지 돌린다.**

1. 정상 거래 — 등록 → 구매 → 확정 → 잔액 확인
2. 만료 실효 — 스케줄러가 처리하는지
3. 예약금 몰수 — 제한시간 초과 → 몰수 → PLATFORM 이동
4. 부분성사 — B 이탈 → A에게 제안 → 크레딧 적용
5. 동시성 — 락 없음 vs 락 적용
6. **최종 정합성 검증 — 위 전부 끝난 뒤 `integrity-check` PASS**

> 6번이 핵심이다. 여기서 틀어지면 어딘가 버그가 있는 것이고,
> 그 버그를 찾는 과정 자체가 보고서에서 가장 좋은 소재가 된다.

**[캡처] 캡처**: `29_최종정합성검증.png`

---

### 11단계 — PDF 보고서

**목차**

```
표지 — 프로젝트명 / 고유번호 / 이름 / 제출일

1. 주제 선정 배경
   1-1. 일상의 불편 — 버려지는 티켓
   1-2. 왜 기존 중고거래로는 안 되는가
   1-3. 교재 구조 대비 차이

2. 시스템 설계
   2-1. 도메인 모델 (엔티티 8개)
   2-2. 카테고리와 만료 유형 — 두 개의 분류 축
   2-3. 상태 전이 다이어그램
   2-4. 패키지 구조

3. 핵심 구현
   3-1. 에스크로와 정산 원장
   3-2. 예약금 — 이탈 방지 장치
   3-3. 1매 대기 매칭
   3-4. 부분 성사 처리
   3-5. 만료 스케줄러

4. 차별화 포인트
   4-1. 동시성 제어 — 락 전/후 비교
   4-2. append-only 원장과 정합성 자가검증
   4-3. 시장 데이터 기반 가격 추천
   4-4. 실시간 소멸 모니터링 대시보드
   4-5. Actuator / AOP / Validation

5. 설계 판단 지점 — 이 프로젝트의 핵심
   (SPEC.md 12장의 7개 항목을 근거와 함께 서술)

6. 검증 결과
   6-1. 시나리오별 검증 (6종)
   6-2. 요구사항 충족 현황 표

7. 한계와 향후 과제
   (SPEC.md 11장 제외 항목 + 제외 사유)
```

**5장이 승부처다.** 코드는 누구나 만들지만, 판단 근거는 직접 정한 사람만 쓸 수 있다.
개발 중에 결정한 것들을 그때그때 메모해 둘 것.

---

## [캡처] 캡처 파일명 규칙

`NN_설명.png` 형태로 번호를 붙여 한 폴더에 모을 것. PDF 작성 속도가 크게 달라진다.

---

## 시간이 부족할 때 버릴 순서

1. 대시보드 차트 (숫자 카드만 남김)
2. MyBatis 통계 3종 → 2종
3. 커스텀 HealthIndicator (기본 Actuator만)
4. 자동확정 스케줄러 (수동 확정만)
5. 기프티콘 EXTENDABLE (카테고리 7종으로)

**절대 버리면 안 되는 것**: 에스크로+원장, 동시성 락 비교, 정합성 검증, 1매 대기
→ 이게 프로젝트의 정체성이다.
