# Claude Code 시작 프롬프트

아래를 **그대로 복사**해서 Claude Code 첫 메시지로 보내세요.
`SPEC.md`와 `PLAN.md`는 프로젝트 폴더 루트에 미리 넣어두세요.

---

## 📋 복사할 내용

```
프로젝트 폴더에 있는 SPEC.md와 PLAN.md를 먼저 읽어줘.

PlanB Market이라는 Spring Boot 프로젝트를 만들 거야.
소멸성 자산(만료 기한이 있는 티켓)의 P2P 양도 플랫폼이고,
SKALA 교육과정 최종 실습 과제로 제출할 거야.

## 작업 방식

1. PLAN.md의 11단계를 순서대로 진행할 건데, 한 번에 하나씩만 해줘.
   내가 "1단계 해줘"라고 하면 1단계만 하고 멈춰. 알아서 다음으로 넘어가지 마.

2. 각 단계가 끝나면 내가 직접 실행해서 확인할 거야.
   PLAN.md에 있는 그 단계의 체크리스트를 마지막에 다시 보여줘.

3. 아래 지점에서는 절대 임의로 결정하지 말고 반드시 나에게 물어봐:
   - SPEC.md에 명시되지 않은 비즈니스 정책 (금액, 시간, 비율, 임계값 등)
   - 동시성 제어 방식 선택
   - 예외 상황에서 누가 손해를 보는지에 대한 판단
   - 엔티티 필드 추가/제거
   - SPEC.md에 없는 라이브러리 도입
   - SPEC.md의 내용과 충돌하는 설계가 필요해 보일 때

   물어볼 때는 선택지와 각각의 장단점을 같이 제시해줘.

4. SPEC.md와 다르게 구현해야 할 이유를 발견하면,
   그냥 바꾸지 말고 먼저 왜 그런지 설명해줘.

5. 나는 코드를 다 이해하고 넘어가려는 게 아니라
   동작 확인과 방향 판단을 할 거야. 그러니까 각 단계 끝에
   "이번에 뭘 만들었고, 어떻게 확인하면 되는지"를 짧게 요약해줘.

## 기술 제약

- Java 21, Spring Boot 3.3.x, Gradle
- JPA + MyBatis 둘 다 사용 (SPEC.md 4-7절 기준으로 역할 분리)
- H2 인메모리
- JWT, Docker, Kubernetes 사용 금지 (과제 요구사항)
- 인증은 HttpSession 기반

## 코드 스타일

- 주석은 한국어 구어체로. 딱딱한 문어체 말고.
- 클래스 상단에 간단한 설명 주석 (이 클래스가 뭘 하는지)
- 복잡한 로직에는 왜 그렇게 했는지 이유를 남길 것
- SQL 키워드와 PK/FK/NULL은 대문자, 나머지 영어 단어는 소문자

## 지금 할 일

SPEC.md와 PLAN.md를 읽고, 이해한 내용을 3~5줄로 요약해줘.
그리고 SPEC에서 애매하거나 빠진 부분이 있으면 알려줘.
아직 코드는 만들지 마.
```

---

## 🔄 이후 단계별로 보낼 메시지

각 단계마다 이렇게 짧게 보내면 됩니다.

```
1단계 해줘.
```

```
확인했어. 2단계 해줘.
```

문제가 있으면:

```
[문제 상황 붙여넣기]
이거 왜 이래?
```

---

## ⚠️ 주의할 점

**"다 만들어줘"라고 하지 마세요.** 한 번에 다 만들면 어디서 틀어졌는지 못 찾습니다.
특히 3단계(에스크로+원장)는 반드시 확인하고 넘어가야 합니다.

**단계 중간에 방향을 바꾸고 싶으면** 바로 말하세요. 뒤로 갈수록 되돌리기 어려워집니다.

**Claude Code가 뭔가를 물어보면** 그게 정상입니다. 위 프롬프트에서 물어보라고 시켰거든요.
답할 때 확신이 없으면 "선택지 장단점 더 자세히 설명해줘"라고 하세요.
