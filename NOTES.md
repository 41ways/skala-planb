# 설계 판단 기록

> 보고서 5장(설계 판단 지점) 재료.
> 코드는 누구나 만들지만 판단 근거는 직접 정한 사람만 쓸 수 있다.
> 각 항목은 **무엇이 문제였는지 → 어떤 선택지가 있었는지 → 왜 그걸 골랐는지** 순서.

---

## 1. 원장을 복식부기로 바꾼 것 ⭐

### 문제
SPEC 1-9의 정합성 규칙 2는 "전체 원장에서 `SUM(CREDIT) == SUM(DEBIT)`"였다.
그런데 `accountId`는 "회원 ID 또는 PLATFORM"뿐이었다.

이 구조로는 규칙이 **첫 실행부터 깨진다.**

- 예치금 충전 → 회원에게 CREDIT 한 줄만 생김. 상대편이 없음
- 예약금 홀드 → 회원에게 DEBIT 한 줄만 생김. 그 돈이 어디 있는지 기록이 없음

시드에 회원 5명 × 50만원만 넣어도 CREDIT이 250만원 초과한다.

### 선택지
| 안 | 내용 | 문제 |
|---|---|---|
| A | 상대계정 3개 도입 (`EXTERNAL`, `ESCROW_POOL`, `DEPOSIT_POOL`) | 원장 줄 수 2배 |
| B | 충전만 `EXTERNAL` 상대계정 | 홀드 시점에 또 깨짐 |
| C | 규칙 2를 "충전·홀드 제외"로 완화 | 검증이 사실상 무의미해짐 |

### 결정 — A
돈이 잠깐 머무는 자리마다 계정을 만들었다. 그러면 **모든 금전 이동이 DEBIT 1줄 + CREDIT
1줄 쌍**이 되고, 전체 차대가 항상 0이 된다. 이 항등식이 깨졌다는 건 곧 코드에 버그가
있다는 뜻이라, 검증이 실제로 무언가를 잡아내는 장치가 된다.

부수 효과가 더 컸다. 원장만 봐도 **지금 시스템 안의 돈이 어디에 얼마나 있는지**가
드러난다. `ESCROW_POOL` 잔액이 구매하면 차오르고 확정하면 비워지는 게 데이터로 보인다.
"3자 보관"이라는 개념이 설명이 아니라 숫자로 증명된다.

그리고 검증 항목이 저절로 늘었다 — `ESCROW_POOL` 잔액과 보관 중인 거래액 합이
같은지, `DEPOSIT_POOL`과 홀드 중인 예약금 합이 같은지. 총액만 보는 규칙 1·2가 못 잡는
"개별 기록이 빠진" 경우를 이쪽이 잡는다.

### 이 결정이 만든 또 다른 결과
`LedgerReason.PLATFORM_INCOME`이 **쓸 데가 없어졌다.** 몰수는 `DEPOSIT_FORFEIT` 하나로
`DEPOSIT_POOL → PLATFORM` 이동이 다 표현된다. 남겨두면 같은 돈 흐름에 사유가 둘이
되어 통계가 헷갈린다. 플랫폼 수익은 사유가 아니라 **`PLATFORM` 계정 잔액으로 구하는
것**이라는 게 명확해졌다.

> 구조를 제대로 잡으면 필요 없는 개념이 저절로 떨어져 나간다는 사례.

---

## 2. 잔액을 바꾸는 통로를 하나로 좁힌 것

### 문제
"잔액 변경은 반드시 원장 기록과 함께"는 지키기 쉬운 규칙이 아니다. 서비스가 늘어날수록
어디선가 `member.balance`만 건드리는 코드가 나온다. 그리고 그런 실수는 정합성 검증에
걸릴 때까지 아무도 모른다.

### 결정
`LedgerService.transfer()`를 **돈이 움직이는 유일한 통로**로 만들었다. 회원 잔액 변경도
이 안에서만 일어난다. 다른 서비스에는 `Member.balance`를 건드리는 코드가 한 줄도 없다.

규칙을 문서로 약속하는 대신 **어길 수 있는 지점 자체를 하나로 줄인 것**이다.
한 곳만 맞으면 전체가 맞는다.

### 관련 — 예치금 충전을 2단계에서 미룬 것
2단계(CRUD)에 `POST /charge`가 있었지만 만들지 않았다. `LedgerService`가 3단계에
나오는데, 그 전에 만들면 잔액만 올라가고 원장이 안 남는다. **규칙을 어기느니 기능을
미루는 쪽**을 택했고, 3단계에서 원장과 함께 붙였다.

---

## 3. 원장 append-only를 타입으로 강제한 것

### 문제
"UPDATE 금지"를 주석으로 적어두면 언젠가 누가 깨뜨린다. 특히 잔액이 틀어졌을 때
"원장을 고치면 되지 않나"라는 유혹이 생긴다.

### 결정
두 겹으로 막았다.

1. `Ledger`에 setter를 아예 안 만들고 생성자를 private으로 잠금 → **코드로 값을 못 바꿈**
2. 모든 컬럼에 `@Column(updatable = false)` → 혹시 바꿔도 하이버네이트가 UPDATE를 안 날림
3. `LedgerRepository`에 수정·삭제 메서드를 만들지 않음

### 왜 append-only인가
잔액만 UPDATE하는 방식이면 숫자가 틀어졌을 때 **원인을 찾을 방법이 없다.**
"지금 5만원인데 왜 5만원인지" 알 수 없다. 기록이 남아 있어야 되짚을 수 있고,
되짚을 수 있어야 검증이 성립한다. 원장은 로그가 아니라 **검증의 근거**다.

---

## 4. 1매 대기 매칭을 걷어낸 것 ⭐

### 문제
SPEC의 핵심 기능 중 하나였던 "2매 티켓을 두 명이 1매씩 나눠 사는" 매칭 구조.
구현하면서 드러난 문제들:

- `Deposit`이 `PairRequest`에 묶여 있어서 **예약금이 1매 대기 전용 기능**이 됨.
  전량 구매하는 사람은 아무 부담 없이 판매자 시간을 묶어둘 수 있음
- 부분 성사(한쪽만 결제) 처리가 이 프로젝트에서 가장 복잡한 로직인데,
  그 복잡도가 만들어내는 가치가 크지 않았음
- `Escrow`가 하나의 `Listing`에 2건 붙을 수 있어서, 소유권 이전·정산·환불 전부에
  "몇 명분인가"라는 분기가 따라붙음

### 결정
매칭을 전부 제거하고 **예약금을 모든 구매의 필수 경로로** 옮겼다.

```
(이전)  구매자 → 즉시 결제                      예약금은 1매 대기에만
(이후)  구매자 → 예약(예약금) → 결제 → 확정      예약금이 본류
```

### 근거
예약금의 본질은 "**남의 시간을 묶어두는 데 드는 값**"이다. 소멸성 자산이라 구매자가
고민하는 동안에도 티켓 가치가 줄고, 그동안 판매자는 다른 구매자를 못 받는다.
그 기회비용을 아무 대가 없이 판매자에게만 지우는 게 불공평한 건 1매 대기든
전량 구매든 똑같다. 애초에 매칭에 종속시킬 이유가 없는 개념이었다.

기능이 하나 줄었지만 **개념 하나가 제 자리를 찾았다.** 엔티티는 8개에서 7개로,
`Escrow`는 `discount` 필드가 사라져 단순해졌고, `Listing`은 `pairable`·`unitPrice`가
빠져 "가격은 희망가 하나"로 정리됐다.

### 대가
- SPEC의 부분 성사 정책(크레딧 50%, 플랫폼 부담)은 통째로 사라졌다
- `Category`에서 `pairable` 축이 빠지면서 **분류 축이 둘에서 하나로** 줄었다.
  "만료 유형과 동반 가능 여부는 서로 다른 축"이라는 SPEC의 관찰은 유효하지만
  이번 범위에서는 만료 유형만 남았다

> 보고서에는 **"기능을 늘리는 판단"만이 아니라 "덜어내는 판단"도 설계**라는 각도로 쓸 것.

---

## 5. 중개 수수료를 넣은 것

### 문제
처음엔 수수료 0%로 잡았다. SPEC 2-3의 금액 예시("판매자 수령 130,000원")와 숫자를
맞추려는 이유였다. 그런데 그러면 **플랫폼 수익이 예약금 몰수밖에 없다.**

이건 구조가 뒤틀린 것이다. 플랫폼이 **이용자 이탈을 반길 유인**이 생긴다.
거래가 잘 성사될수록 손해인 플랫폼은 말이 안 된다.

### 결정 — 5%
정산 시점에 이체를 두 줄로 쪼갰다.

```
ESCROW_POOL → PLATFORM     7,920   COMMISSION
ESCROW_POOL → 판매자     150,480   SELLER_SETTLE
```

`LedgerReason.COMMISSION` 하나와 상수 하나가 추가 비용의 전부다.
1절에서 뺐던 `PLATFORM_INCOME` 자리를 **제대로 된 이름으로** 채운 셈이기도 하다.

### 부수 효과
원장 데모가 강해졌다. 수수료가 없으면 `PLATFORM` 계정은 평소에 0이고 몰수가 일어날
때만 움직인다. 수수료가 있으면 **성격이 다른 두 수입 경로**(거래 성사 / 이탈)가
생겨서 정합성 검증이 실제로 뭔가를 검증하게 된다.

### SPEC과의 충돌
SPEC 2-3의 금액 예시와 숫자가 어긋난다. 하지만 그 조항의 핵심은
"부분성사 할인분을 판매자에게 떠넘기지 않는다"이고, 정률 수수료는 상황과 무관하게
똑같이 붙으므로 그 원칙은 그대로다. 시드의 과거 거래 15건에도 소급 적용해서
**지금 규칙과 과거 데이터가 따로 놀지 않게** 했다.

---

## 6. 청약철회 10분을 두 곳에 똑같이 쓴 것

### 문제
SPEC은 예약금 취소에만 "10분 내 전액 환불" 규칙이 있었다. 에스크로 환불
(`POST /api/escrows/{id}/refund`)에는 **조건이 아예 없었다.**

조건이 없으면 구매자가 자동 확정 전까지 언제든 물릴 수 있다. 판매자는 최대 24시간
묶여 있다가 환불당하고, 그 사이 다른 구매자를 놓치고 만료만 가까워진다.

### 선택지
| 안 | 내용 | 문제 |
|---|---|---|
| A | 결제 후 10분 이내만 | 구매자 보호가 약해짐 |
| B | 자동확정 전까지 언제든 | 에스크로에만 이탈 비용이 없어 불균형 |
| C | 환불 API 자체를 제거 | SPEC의 API가 사라짐 |

### 결정 — A
예약금 취소와 **같은 10분**을 썼다. 규칙이 하나면 설명도 하나로 끝난다.

더 중요한 건 일관성이다. 예약 단계에는 이탈 비용(몰수)이 있는데 결제 단계에는
없다면, 사용자는 "일단 결제하고 천천히 생각하자"를 학습한다. 그게 정확히
막으려던 행동이다.

---

## 7. 자동확정을 만료보다 앞당긴 것 ⭐

### 문제
SPEC의 자동확정 시각은 `min(결제 후 24시간, ticket.expiresAt)`이었다.
만료가 24시간 안에 오는 티켓이면 **자동확정 시각 == 만료 시각**이 된다.

그 순간 두 스케줄러가 같은 건을 놓고 만난다.
- 자동확정 스케줄러 → `CONFIRMED`, 판매자에게 정산
- 만료 스케줄러 → `VOIDED`, 구매자에게 전액 환불

정반대 처리다. 게다가 만료 스케줄러가 1분 주기로 더 자주 도니까 **사실상 항상 만료가
먼저 잡힌다.** 자동확정이 영영 안 일어나고, 판매자는 티켓을 넘겼는데 돈을 못 받는다.

### 선택지
| 안 | 내용 | 문제 |
|---|---|---|
| A | 자동확정을 만료 10분 전으로 당김 | 만료 임박에 결제한 경우가 남음 |
| B | 만료 우선 — `IN_ESCROW`면 무조건 환불 | 판매자가 티켓만 잃음 |
| C | 스케줄러 실행 순서로 해결 | 순서에 의존하는 설계는 깨지기 쉬움 |

### 결정 — A
```java
autoConfirmAt = min(결제 + 24시간, 만료 − 10분)
```

**"어느 쪽이 우선인가"를 정하는 것 자체가 문제였다.** 부딪히지 않게 만들면 우선순위
규칙이 필요 없어진다. 스케줄러 실행 순서를 조정할 필요도, 락을 걸 필요도 없다.

근거도 명확하다 — 구매자는 결제 직후부터 티켓을 쓸 수 있었고, 안 쓴 건 판매자
잘못이 아니다. 만료는 그 결과일 뿐이다.

자동확정 스케줄러를 5분 주기로 둔 것도 여기서 나온다. 10분 여유가 있으니 최악의
경우 5분 늦어도 아직 만료 5분 전이다. **구조적으로 부딪힐 수 없다.**

> 이 항목은 "정책을 정하는 것"과 "정책이 필요 없게 만드는 것"의 차이를 보여주는
> 사례로 쓸 것.

---

## 8. 마감 임박 알림을 고정분이 아니라 비율로 잡은 것

### 문제
결제 제한시간은 `min(30분, 남은시간 × 0.5)`이라 티켓마다 다르다. 넉넉한 티켓은 30분,
만료가 2시간 남았으면 30분, 만료가 8분 남았으면 4분이다.

여기에 "마감 5분 전 알림"을 걸면 제한시간이 4분인 건에서는 **알림 시점이 이미 지나
있다.** 예약하자마자 알림이 나가거나 아예 안 나간다. 그리고 그런 건이야말로 제일
급한 건이다.

### 결정
**주어진 시간의 절반 지점**에 1회.

```java
needsDeadlineWarning(now) = now >= heldAt + (paymentDeadline - heldAt) / 2
```

30분이면 15분 남았을 때, 4분이면 2분 남았을 때. 어떤 티켓이든 "절반 썼을 때"라는
일관된 경험이 된다.

**시간이 티켓마다 다른 도메인에서는 절대값 기준이 잘 안 맞는다**는 게 요지다.
결제 제한시간 자체를 비율로 잡은 것과 같은 발상.

---

## 9. 만료 시점의 처리를 진행 단계로 나눈 것

### 문제
티켓이 만료될 때 묶여 있던 돈을 어떻게 할 것인가. 누구에게도 귀책이 없는 상황이다.

### 결정
그 순간 **어디까지 진행됐는가**로 갈랐다.

| 상태 | 처리 | 근거 |
|---|---|---|
| `OPEN` | 티켓만 실효 | 아무도 손대지 않았음. 되돌릴 돈이 없음 |
| `RESERVED` | 예약금 전액 환불 | 제한시간 안이었음. 구매자 잘못이 아니라 시간이 다한 것 |
| `IN_ESCROW` | 결제금 전액 환불 | 확정만 기다리던 중. 못 쓰는 티켓 값을 판매자가 가질 수 없음 |
| `COMPLETED` | 처리 없음 | 이미 끝난 거래 |

`RESERVED`와 `IN_ESCROW`를 둘 다 환불로 정한 근거: 제한시간 안에 결제했고 확정을
기다리는 중이었다면 **구매자는 할 일을 다 했다.** 반대로 판매자도 티켓을 내놓았을
뿐 잘못한 게 없다. 어느 쪽에도 귀책이 없을 때는 **돈이 원래 자리로 돌아가는 게
가장 덜 불합리하다.**

몰수는 "할 수 있었는데 안 한 경우"에만 쓴다 — 제한시간을 넘긴 것, 10분이 지난 뒤
마음을 바꾼 것. 그 구분이 예약금 정책 전체를 관통한다.

---

## 10. 몰수분을 판매자가 아니라 플랫폼에 준 것

### 문제
예약금을 몰수하면 그 돈은 어디로 가야 하나. 시간을 날린 건 판매자다.

### 결정 — 플랫폼
판매자에게 주면 **판매자가 이탈을 유도할 유인**이 생긴다. 결제하기 어렵게 만들거나
연락을 피하면 몰수분을 챙길 수 있게 된다.

그리고 판매자가 실제로 잃은 건 시간이지 돈이 아니다. 예약금만큼의 금전 손해를 본 게
아닌데 그만큼을 받는 건 근거가 없다.

> 5절(수수료)과 짝이 되는 판단. **수익 경로가 몰수뿐이면 플랫폼이 이탈을 반기고,
> 몰수가 판매자에게 가면 판매자가 이탈을 유도한다.** 어느 쪽이든 구조가 뒤틀린다.

---

## 11. 회원 삭제를 막은 것

### 문제
`DELETE /api/members/{id}`. 원장이 남아 있는 회원을 지우면 어떻게 되나.

원장은 append-only라 회원을 지워도 기록은 그대로 남는다. 그러면 **"회원 없는 계정"**이
생겨서 정합성 규칙 1(회원 잔액 == 원장 합)이 검사할 대상을 잃는다.

### 선택지
| 안 | 내용 | 문제 |
|---|---|---|
| A | 거래 이력 있으면 409 거부 | 시드 회원 5명은 전부 삭제 불가 |
| B | soft delete (`deletedAt`) | 필드 추가 + 모든 조회에 조건이 붙음 |
| C | 하드 삭제 그대로 | 정합성 검증 붕괴 |

### 결정 — A
원장 기록이 하나라도 있으면 삭제를 막는다. 갓 가입해서 아무것도 안 한 회원은 정상
삭제되니 API가 죽지는 않는다.

근거: **돈이 오간 흔적을 지울 수 없다는 게 원장을 쌓는 이유**다. 그렇다면 그 흔적의
주인을 지우는 것도 막는 게 일관된다. soft delete는 같은 결과를 더 복잡하게 얻는
방법이고, 조회마다 "안 지워진 것만" 조건이 붙어서 빠뜨리면 버그가 된다.

---

## 12. MyBatis와 JPA의 경계 (7단계 예정, 기준은 확정)

### 기준
"조회가 복잡한가"가 아니라 **"여러 행을 집계해서 줄여야 하는가"**로 나눈다.

| 대상 | 도구 | 이유 |
|---|---|---|
| 가격 추천 / 카테고리 현황 / 일별 실효 손실 | MyBatis | 여러 테이블을 조인해 `GROUP BY`로 행을 줄이고 `CASE WHEN`으로 구간을 나눔 |
| 정합성 검증의 계정별 합계 | JPA | `GROUP BY` 하나짜리라 MyBatis까지 갈 이유가 없음. 원장 저장소에 붙어 있는 게 자연스러움 |
| 회원 상세, 목록 조회 | JPA | 엔티티를 그대로 쓰는 조회 |

**경계 사례를 놓고 판단한 것 자체가 설계 결과물**이다. 정합성 검증도 집계지만
JPA로 남긴 이유는, "원장이 스스로를 검증한다"는 성격이라 원장 저장소에 있는 게
읽기 좋고 쿼리가 단순해서다.

---

## 13. 개발 중 만난 버그들 (보고서 6장 재료)

### 13-1. 같은 뿌리의 버그를 두 번 만난 것 ⭐

**1차 — 기한 연장 후 `expiresAt`이 안 바뀜 (3단계)**
기프티콘 기한을 연장했는데 응답의 `expiresAt`이 옛날 값이었다. DB에는 제대로 들어갔다.

원인은 `@PreUpdate`의 실행 시점. 만료 시각 계산을 저장 콜백에 맡겼는데, 콜백은
flush 때 돌고 응답 DTO는 그 전에 만들어진다.

**2차 — 예약 등록 응답에 `depositAmount`가 `null` (4단계)**
잔액은 정확히 빠졌는데 응답만 비어 있었다.

원인은 양방향 연관의 한쪽만 세팅된 것. FK는 `Deposit`이 들고 있어서 DB엔 제대로
저장되지만, 메모리에 떠 있는 상대 객체의 필드는 그대로 `null`이다. JPA가 역방향을
자동으로 채워주지 않는다.

**공통점**
둘 다 **"JPA가 알아서 해주겠지"라고 가정한 지점**에서 터졌고, 둘 다 DB는 맞는데
응답만 틀렸다. 그래서 로그나 DB만 봐서는 안 보인다.

두 번째는 첫 번째 덕에 증상을 보자마자 원인을 짚었다.
**한 번 겪은 실패 유형은 두 번째부터 훨씬 싸다**는 게 실감난 부분.

### 13-2. `@OneToOne` unique 제약이 재등록을 막은 것 (6단계)
`Listing.ticket`에 unique 제약이 걸려 있었다. 판매를 철회하면 티켓이 `OWNED`로
돌아가 재등록이 가능한데, 저장할 때 제약 위반으로 터진다.

**6단계에서 만료 스케줄러가 티켓으로 판매 건을 역추적하면서 드러났다.**
그전까지는 아무도 재등록 경로를 밟지 않아서 잠복해 있었다.

`@ManyToOne`으로 바꾸고 "동시에 살아 있는 판매은 하나뿐"은 티켓 상태로 보장하게 했다.

> **스케줄러가 회귀 테스트 역할을 했다**는 게 흥미로운 지점. 사람이 안 밟는 경로를
> 기계가 밟으면서 잠복 버그가 드러났다.

### 13-3. 검증 순서 때문에 기대와 다른 응답이 나온 것
"만료된 티켓 예약"을 테스트했는데 400 `TICKET_EXPIRED`가 아니라 400
`SELF_TRADE_NOT_ALLOWED`가 나왔다. 그 판매 건의 판매자가 테스트 계정이었고,
본인 티켓 검사가 만료 검사보다 먼저였다.

비로그인 상태로 티켓을 등록해봤을 때도 401이 아니라 400이 나왔다. 빈 본문을 보내서
세션 검사보다 Bean Validation이 먼저 걸린 것이다.

**둘 다 앱은 정상이고 테스트 기댓값이 틀렸다.** 검증 순서가 응답을 좌우한다는 걸
보여주는 사례라 보고서에 짧게 넣을 만하다.

---

## 14. 검증을 스크립트로 옮긴 것

### 문제
단계마다 손으로 API를 호출해 잔액을 확인하고 있었다. 단계가 쌓일수록 앞 단계가
안 깨졌는지 다시 확인하는 비용이 커졌다.

그리고 실제로 **5단계 재설계로 그때까지 찍어둔 캡처의 절반 이상이 무효화**됐다.
`purchase` API가 사라지고 Swagger 태그·엔드포인트가 바뀌면서, 이미 들인 시간이
그대로 날아갔다.

### 결정
`scripts/verify.sh`에 전 시나리오를 담았다. 지금 55개 검증.

두 가지를 겸한다:
1. **회귀 테스트** — 6단계에서 스케줄러를 붙인 뒤 3단계 정산이 안 깨졌는지 확인
2. **캡처 가이드** — 📸 표시가 뜨는 지점이 곧 화면을 찍을 자리

캡처도 **8단계 끝과 10단계 두 번**으로 몰았다. 근거: 9·10단계는 기존 API를 안
건드리므로 8단계에서 찍은 건 끝까지 살아남는다. 그리고 한 번의 앱 실행에서 찍으면
잔액이 처음부터 끝까지 하나로 이어져서 보고서에서 읽기 좋다.

> "작업을 언제 하느냐"도 설계 판단이라는 각도. 완성 전에 산출물을 만들면
> 설계가 바뀔 때마다 그만큼을 다시 만들어야 한다.

---

## 15. 테스트 전용 API를 만들지 않은 것

### 문제
청약철회 10분, 결제 제한시간 초과를 검증하려면 시간을 앞으로 돌려야 한다.
실시간으로 기다릴 수는 없다.

### 선택지
| 안 | 내용 | 문제 |
|---|---|---|
| A | H2 콘솔로 컬럼을 직접 수정 | 검증 스크립트가 DB 내부에 의존 |
| B | 테스트 전용 API 추가 (`/api/test/time-travel`) | 운영에 나가면 안 되는 구멍이 생김 |
| C | 시계를 추상화 (`Clock` 주입) | 전 서비스에 `Clock`이 퍼짐 |

### 결정 — A
검증 스크립트에서만 H2 콘솔로 `held_at`·`payment_deadline`을 조정한다.

B는 앱에 "시간을 조작할 수 있는 입구"를 남기는 것이라 택하지 않았다. 프로파일로
막을 수 있지만, 그 자체가 관리해야 할 위험이 하나 늘어나는 일이다.

C가 정석이긴 하다. 다만 모든 서비스에 `Clock`을 주입하는 비용 대비, 이 프로젝트에서
시간 의존 로직이 몰려 있는 곳은 몇 군데뿐이라 과했다.

**우회로를 택했다는 사실과 그 이유를 스크립트 주석에 남겨두는 것**으로 정리했다.
나중에 규모가 커지면 C로 가는 게 맞다.

---

## 부록 — 보고서 5장 구성 제안

| 절 | 항목 | 강도 |
|---|---|---|
| 5-1 | 원장을 복식부기로 (1절) | ⭐⭐ |
| 5-2 | 잔액 변경 통로를 하나로 (2절) + append-only 강제 (3절) | ⭐⭐ |
| 5-3 | 1매 대기를 걷어낸 것 — 덜어내는 판단 (4절) | ⭐⭐ |
| 5-4 | 자동확정을 앞당겨 충돌을 없앤 것 (7절) | ⭐⭐ |
| 5-5 | 수수료와 몰수 — 유인 구조 설계 (5절 + 10절) | ⭐ |
| 5-6 | 귀책 없는 상황의 돈 처리 (9절) | ⭐ |
| 5-7 | MyBatis / JPA 경계 (12절) | ⭐ |

6장(검증 결과)에는 13절의 버그 세 건을, 7장(한계와 향후 과제)에는 15절과
`PROGRESS.md`의 "알려진 이슈"를 옮겨 쓰면 된다.
